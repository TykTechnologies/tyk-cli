# STK-REQ-001: As an API operator I can manage the full lifecycle of OAS-native APIs — list, retrieve, create, import, update, apply idempotently, and delete — without leaving the terminal

<!-- reqproof:req STK-REQ-001 -->

## Intent
As an API operator I can manage the full lifecycle of OAS-native APIs — list, retrieve, create, import, update, apply idempotently, and delete — without leaving the terminal

## Context
- Level: Stakeholder
- Component: api
- Type: guarantee
- Category: functional
- Status: approved
- Assurance: B

## Rationale
Operators run automation pipelines that provision and reconcile APIs on the Tyk Dashboard. A complete CLI lifecycle reduces toil and enables GitOps-style workflows.

## Source
See `specs/stakeholder/requirements/STK-REQ-001.req.yaml` for the machine-readable definition.
